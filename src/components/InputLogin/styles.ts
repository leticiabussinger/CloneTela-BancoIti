import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        width: 32,
        height: 47,
        alignItems: 'center',
        justifyContent: 'center',
        borderRadius: 10,
        overflow: 'hidden',
        marginLeft: 8,
        marginRight: 8
    },
    input: {
        width: 30,
        height: 45,
        textAlign: 'center',
        fontSize: 18,
        backgroundColor: "#fff",
        borderRadius: 9
    }
});